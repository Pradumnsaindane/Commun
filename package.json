{
  "name": "commun-com",
  "version": "0.1.0",
  "private": true,
  "description": "Commun.dev workspace",
  "scripts": {
    "dev": "npm --prefix commun.dev run dev --",
    "build": "npm --prefix commun.dev run build --",
    "preview": "npm --prefix commun.dev run preview --",
    "install:app": "npm --prefix commun.dev install"
  }
}
